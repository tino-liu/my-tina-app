name: Build & deploy Next.js app to Azure Web App

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: [main]

  # Allow manual workflow trigger
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🌱 Checkout to the branch
        uses: actions/checkout@v4

      - name: 🍏 Set up Node.js version
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: 📏 Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: 🔑 Create .env file
        run: |
          cat <<'EOF' > .env
          NEXT_PUBLIC_TINA_CLIENT_ID=${{ secrets.NEXT_PUBLIC_TINA_CLIENT_ID }}
          TINA_TOKEN=${{ secrets.TINA_TOKEN }}
          NEXT_PUBLIC_TINA_BRANCH=${{ secrets.NEXT_PUBLIC_TINA_BRANCH }}
          NEXT_PUBLIC_SITE_URL=${{ secrets.NEXT_PUBLIC_SITE_URL }}
          EOF

      - name: ⚙️ Install dependencies and build
        run: |
          pnpm install
          pnpm run build

      - name: Copy static files and configuration
        run: |
          cp -R ./public ./.next/standalone/public
          cp -R ./.next/static ./.next/standalone/.next/static
          cp package.json ./.next/standalone/package.json
          cp web.config ./.next/standalone/web.config
          cp .deployment ./.next/standalone/.deployment

      - name: 📦 Create deployment archive with symlinks preserved
        run: |
          cd ./.next/standalone
          tar -czf ../../deploy.tar.gz .

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: app
          path: ./deploy.tar.gz

      - name: 🔍 Diagnostics
        run: |
          ls -al
          ls -la ./.next
          ls -la ./.next/standalone 2>/dev/null || echo "No standalone directory found"
          find ./.next -name "server.js" 2>/dev/null || echo "No server.js found"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: Production

    steps:
      - name: 🍏 Set up Node.js version
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: app
          path: ./

      - name: Extract deployment archive
        run: |
          mkdir -p app
          tar -xzf deploy.tar.gz -C app
          rm deploy.tar.gz
          echo "=== Deployment contents ==="
          ls -la app/
          echo "=== Checking for server.js ==="
          find app/ -name "server.js" -o -name "package.json" | head -10

      - name: 🗝️ Azure Login
        uses: azure/login@v2.2.0
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          environment: AzureChinaCloud

      - name: 🚀 Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3.0.1
        with:
          app-name: ${{ secrets.AZURE_APP_SERVICE_NAME }}
          package: ./app
          startup-command: "node server.js"